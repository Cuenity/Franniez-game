using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class TestAspectRatio : MonoBehaviour
{
    public Text Play;
    public Text Store;
    public Text Settings;

    //public Camera Camera;

    // Start is called before the first frame update
    void Start()
    {
        //if (Camera.main.aspect >= 2)
        //{
        //    RectTransform rectTransform;
        //    rectTransform = Play.GetComponent<RectTransform>();
        //    rectTransform.localPosition = new Vector3(136, -3, 0);
        //    rectTransform.localScale = new Vector3(0.8f,0.8f,0);

        //    rectTransform = Store.GetComponent<RectTransform>();
        //    rectTransform.localPosition = new Vector3(-115.7f, -31, 0);
        //    rectTransform.localScale = new Vector3(0.9f, 0.9f, 0);

        //    rectTransform = Settings.GetComponent<RectTransform>();
        //    rectTransform.localPosition = new Vector3(-119.29f, 53, 0);
        //    rectTransform.localScale = new Vector3(0.8f, 0.8f, 0);
        //}
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
